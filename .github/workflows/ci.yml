---
name: CI

on:
  push:

jobs:
  Lint:
    runs-on: ubuntu-latest
    steps:
      - run: echo "example"
      - run: echo "does it work ?"
  
  Static Code Analysis Tests:
    runs-on: ubuntu-latest
    need:
      - Lint
    steps:
      - run: echo "example"
      - run: echo "does it work ?"

  Unit Tests:
    runs-on: ubuntu-latest
    need:
      - Static Code Analysis Tests
    steps:
      - run: echo "example"
      - run: echo "does it work ?"

  Build:
    runs-on: ubuntu-latest
    need:
      - Unit Tests
    steps:
      - run: echo "example"
      - run: echo "does it work ?"

  Integ Tests:
    runs-on: ubuntu-latest
    need:
      - Build
    steps:
      - run: echo "example"
      - run: echo "does it work ?"
